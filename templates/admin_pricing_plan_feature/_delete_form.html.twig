<form method="post" action="{{ path('app_admin_pricing_plan_feature_delete', {'id': pricing_plan_feature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pricing_plan_feature.id) }}">
    <button class="btn">Delete</button>
</form>
