

{% block title %}PricingPlan{% endblock %}

{% block body %}
    <h1>PricingPlan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pricing_plan.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pricing_plan.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ pricing_plan.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_pricing_plan_index') }}">back to list</a>

    <a href="{{ path('app_admin_pricing_plan_edit', {'id': pricing_plan.id}) }}">edit</a>

    {{ include('admin_pricing_plan/_delete_form.html.twig') }}
{% endblock %}
