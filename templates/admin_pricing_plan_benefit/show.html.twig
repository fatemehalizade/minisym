

{% block title %}PricingPlanBenefit{% endblock %}

{% block body %}
    <h1>PricingPlanBenefit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pricing_plan_benefit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pricing_plan_benefit.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_pricing_plan_benefit_index') }}">back to list</a>

    <a href="{{ path('app_admin_pricing_plan_benefit_edit', {'id': pricing_plan_benefit.id}) }}">edit</a>

    {{ include('admin_pricing_plan_benefit/_delete_form.html.twig') }}
{% endblock %}
