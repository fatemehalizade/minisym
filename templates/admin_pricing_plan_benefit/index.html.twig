

{% block title %}PricingPlanBenefit index{% endblock %}

{% block body %}
    <h1>PricingPlanBenefit index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pricing_plan_benefit in pricing_plan_benefits %}
            <tr>
                <td>{{ pricing_plan_benefit.id }}</td>
                <td>{{ pricing_plan_benefit.name }}</td>
                <td>
                    <a href="{{ path('app_admin_pricing_plan_benefit_show', {'id': pricing_plan_benefit.id}) }}">show</a>
                    <a href="{{ path('app_admin_pricing_plan_benefit_edit', {'id': pricing_plan_benefit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_pricing_plan_benefit_new') }}">Create new</a>
{% endblock %}
